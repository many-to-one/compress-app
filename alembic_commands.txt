alembic revision --autogenerate -m "message"
alembic upgrade head

# delete all migrations
rm -rf alembic/versions/*
alembic revision --autogenerate -m "init"
alembic upgrade head
# if some errors:
# ERROR [alembic.util.messaging] Can't locate revision identified by '242d63a77966':
docker exec -it container_name psql -U my_db_user -d my_db
SELECT * FROM alembic_version;
# should be:
 version_num
--------------
 242d63a77966
# than:
DELETE FROM alembic_version;

- tak:
alembic revision --autogenerate -m "init"
alembic upgrade head
- albo tak (żeby zachować migracje alembic)
alembic stamp head
alembic upgrade head


# how to connect to db via docker:
docker exec -it container_name psql -U my_user -d my_db

# odblokować IP:
UPDATE users
SET blocked_ips = array_remove(blocked_ips, '***.24.0.1')
WHERE id = 1; (lub WHERE email = 'xxx@gmail.com';)

# odblokować wszystkie IP na raz:
UPDATE users SET blocked_ips = '{}' WHERE email = 'xxx@gmail.com';

# odblokować użytkownika:
UPDATE users SET is_blocked = FALSE WHERE email = 'xxx@gmail.com';
